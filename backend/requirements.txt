# ── Web framework / server ────────────────────────────────────────
fastapi==0.115.0
uvicorn[standard]==0.30.6
sse-starlette==2.1.3

# ── Config / settings (.env parsing) ──────────────────────────────
pydantic-settings==2.5.2
python-dotenv==1.0.1

# ── Database (local PostgreSQL) ───────────────────────────────────
psycopg2-binary==2.9.9

# ── Attachment storage (S3 on AWS; local disk when bucket unset) ──
boto3==1.35.36

# ── LLM / agent framework ─────────────────────────────────────────
# Claude Messages API + web_search server tool (vessel library enrichment)
anthropic==0.49.0
httpx==0.27.2
langgraph==0.2.35
# openai / NVIDIA NIM — removed; Claude is the only LLM client

# ── Time zones (Home / inbox calendar day scope) ──────────────────
# Needed on Windows and some slim images for ZoneInfo("Asia/Kolkata") etc.
tzdata==2025.2

# ── Attachment parsing (Claude reads images/PDF natively) ─────────
Pillow==10.4.0          # image downscaling / format conversion
openpyxl==3.1.5         # Excel (.xlsx) → text
python-docx==1.1.2      # Word (.docx) → text
xlrd==2.0.1             # Excel .xls owners directory upload
python-multipart==0.0.12  # owners directory file upload
